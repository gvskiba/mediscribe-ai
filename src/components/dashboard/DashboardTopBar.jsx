/**
 * DashboardTopBar — v2 stub.
 * The top bar is now fully handled by layout.jsx (v2 merged top bar).
 * This component renders nothing to prevent double bars.
 */
export default function DashboardTopBar() {
  return null;
}